@{
  Layout = "_Layout";
}

@using PierreOrder.Models;

<h1>Orders</h1>
  @if (Model.Count == 0)
  {
    <p>There are no orders in the list.</p>
  }
    <ul>
    @foreach (Order order in Model)
    {
      <li><a href='/orders/@order.Id'>@order.Title</a></li>
    }
  </ul>
  <a href="/orders/new">Add a new order</a>